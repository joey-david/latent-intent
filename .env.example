# Copy this to .env on the SSH server and fill in the token before running.
HF_TOKEN=

# Optional overrides. The defaults are also in configs/default.yaml.
# MODEL_NAME=Qwen/Qwen2.5-1.5B-Instruct
# RUN_NAME=temporal-latent-intent-qwen15b
# HF_HOME=/tmp/hf-cache
# TRANSFORMERS_CACHE=/tmp/hf-cache
